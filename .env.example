# OAuth Client Environment Configuration

# ======================================================================
# SECURITY CONFIGURATION
# ======================================================================

# Token Encryption Key (REQUIRED for production)
# This key is used to encrypt stored OAuth tokens for security.
#
# Option 1: Direct 32-byte key (recommended for production)
# - Must be exactly 32 bytes (256 bits) for AES-256-GCM encryption
# - Can be provided in hex format (64 characters) or base64 format (44 characters)
# - Generate a secure random key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# - Or base64: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# Option 2: Password-based key derivation (KDF)
# - Prefix with 'kdf:' followed by a strong password
# - Uses PBKDF2 with 100,000 iterations to derive a 32-byte key
# - Example: TOKEN_ENCRYPTION_KEY=kdf:your-very-strong-password-here
#
# Examples (DO NOT use these in production):
# TOKEN_ENCRYPTION_KEY=1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
# TOKEN_ENCRYPTION_KEY=kdf:my-super-secure-password-for-development

TOKEN_ENCRYPTION_KEY=

# ======================================================================
# APPLICATION CONFIGURATION
# ======================================================================

# Environment (development, production, test)
NODE_ENV=development

# Logging level (error, warn, info, debug)
LOG_LEVEL=info

# ======================================================================
# OAUTH PROVIDER CREDENTIALS
# ======================================================================
# Add your OAuth provider credentials here
# Never commit actual secrets to version control!

# Example for Google OAuth
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_REDIRECT_URI=http://localhost:3000/callback

# Example for GitHub OAuth
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret
# GITHUB_REDIRECT_URI=http://localhost:3000/callback

# Example for Microsoft OAuth
# MICROSOFT_CLIENT_ID=your-microsoft-client-id
# MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret
# MICROSOFT_REDIRECT_URI=http://localhost:3000/callback

# ======================================================================
# DEVELOPMENT CONFIGURATION
# ======================================================================

# CLI Server port (for callback handling)
CLI_SERVER_PORT=3000

# Default provider for CLI commands
DEFAULT_PROVIDER=google

# Token storage directory (optional, defaults to ~/.oauth-client/tokens)
# TOKEN_STORAGE_DIR=/custom/path/to/tokens